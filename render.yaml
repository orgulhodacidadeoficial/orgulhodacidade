services:
  - type: web
    name: orgulho-da-cidade
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    routes:
      - path: /
        matchType: prefix
        dest: /index.html

